linux_server:
  host: "${LINUX_HOST:-192.168.138.129}"
  port: 22
  username: "${LINUX_USER:-root}"
  password: "${LINUX_PASSWORD}"  # 从环境变量读取，禁止硬编码

linux_service_check:
  check_commands:
    java: "java -version"
    docker: "docker --version"
    nginx: "systemctl status nginx"